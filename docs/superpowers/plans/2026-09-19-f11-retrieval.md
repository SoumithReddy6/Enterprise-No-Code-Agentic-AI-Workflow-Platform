# F11 retrieval and evaluation plan

**Goal:** Recover regulatory passages without changing generation models, prompts, contracts, labels, or quality thresholds.

**Architecture:** Add an opt-in section-aware paragraph strategy with inherited heading metadata and list boundaries. Embed heading plus body. Search gathers 50 candidates and optionally ranks them with a local ONNX cross-encoder before top-k. Keep authority checks after ranking. Existing indexes require a rebuild to gain headings; old chunking options remain available.

**Spec:** User's four-step F11 request in this task. Implementation order is 2, 3, 1, 4.

- [x] Add a failing safety-gate test, implement the ≥0.9 unanswerable abstention check, verify the existing artifact fails both named gates.
- [x] Add a failing artifact test and preserve every retrieved passage's citation, filename, page, and text.
- [x] Save a fresh before baseline on all four search modes, including complete generation evidence.
- [x] Test headings across pages, nested regulation markers, intact list items, legacy metadata migration, reranking before top-k, filtering, and unavailable-model errors.
- [x] Implement `section` chunking, heading metadata migration and embedding text, local reranking, candidate pool 50, API/node/UI/eval options.
- [x] Run all four retrieval modes after rebuilding and rerun unchanged generation. Report the four named misses individually and preserve unmet gates.
- [x] Annotate 20–30 answers against saved evidence, including the four faulty final-batch answers. Clearly distinguish assistant annotations from independent human labels. Run the existing judge unchanged and report compliance, false acceptance/rejection, and coverage. Do not wire judge output into runtime or release gates.
- [x] Run backend and frontend regression checks; write a report with before/after settings and artifact paths. No commit or push.

Measured result: retrieval 90.9% remains below 95%; generation substring match 90.9% passes, but unanswerable abstention 40% fails the new 90% gate. This is an experimental configuration, not an accepted release. See [report](../../f11-retrieval-calibration-report.md).

Calibration completed: 5/24 valid judgments, 19 errors; conditional false acceptance 0/4 and false rejection 0/1 are not reliable estimates at this coverage. Independent human label review remains pending. The reviewer found ambiguous alphabetic/Roman ancestry; two regression tests and a conservative scope fix were added, and the final real-corpus rerun is complete (f11-after-review.json), with unchanged retrieval and abstention rates.
